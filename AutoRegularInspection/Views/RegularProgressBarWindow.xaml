<Window x:Class="AutoRegularInspection.Views.RegularProgressBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoRegularInspection.Models"
        mc:Ignorable="d"
        Title="处理进度" Height="200" Width="300">
    <Window.DataContext>
        <local:ProgressBarModel/>
    </Window.DataContext>

    <Window.Resources>
        <Thickness x:Key="OuterMargin" >20</Thickness>
        <Thickness x:Key="InnerMargin" >10</Thickness>
    </Window.Resources>

    <StackPanel Margin="{StaticResource OuterMargin}">
        <Grid Height="35">
            <ProgressBar Name="progressBar" Value="{Binding ProgressValue}" Foreground="#FF2EAFF1" />
            <TextBlock Name="progressBarNumberTextBlock" Text="{Binding ProgressValue, StringFormat={}{0}%}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        <Grid Height="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="80" />
            </Grid.ColumnDefinitions>

            <Button Content="取消" Command="{Binding CancelCommand}" Width="70" Height="24"  HorizontalAlignment="Left" VerticalAlignment="Top" Margin="90,6,0,0"  />
        </Grid>
        <TextBlock Name="progressBarContentTextBlock" Margin="{StaticResource InnerMargin}" Text="{Binding Content}" HorizontalAlignment="Left"  TextWrapping="Wrap"/>
        <Label  Content="取消操作仅能取消报告生成" Height="30" Width="180" />

    </StackPanel>
</Window>
